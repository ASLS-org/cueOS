#include "DMX512_scene.h"

void DMX512_scene_trigger(DMX512_scene_s *scene){
//	DMX512_fixture_s *fixture;
//	for(uint16_t i=0; i< scene->fCount; i++ ){
//		DMX512_scene_fSetting_s fSetting = scene->fSettings[i];
//		DMX512_patcher_getFixtureInstance(fixture, fSetting->fixtureId);
//		for(uint16_t j=0; i< sizeof(fSetting->); i++ ){
//		fixture->channels[]
//	}
}
